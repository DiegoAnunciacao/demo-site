<section class="pt-24 md:pt-28 lg:pt-32 bg-default">
  <div class="container mx-auto py-20 px-8">
    <h1 class="title mb-2">Products & Services</h1>
    <p class="text-center text-foreground max-w-2xl mx-auto mb-12" >Carefully curated offerings to preserve your wedding memories in the most beautiful way.</p>

    <div class="grid grid-cols md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @products.each do |product| %>
        <div class="bg-white rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-shadow duration-300">
          <div class="aspect-[4/3] overflow-hidden">
            <%= image_tag url_for(product.image), class: "w-full h-full object-cover transition-transform duration-500 hover:scale-105" %>
          </div>
          <div class="p-5 space-y-3">
            <h3 class="text-xl font-medium"><%= product.title %></h3>
            <p class="text-gold-dark font-medium">From €<%= product.price %></p>
            <p class="text-foreground text-sm"><%= product.description %></p>
            <%= link_to "Enquire Now", pages_contact_path, class: "btn-card" %>
          </div>
        </div>
      <% end %>
    </div>
    <div class="mt-16">
      <h2 class="text-center text-2xl md:text-3xl mb-6">Custom Packages</h2>
      <p class="text-foreground text-center max-w-2xl mx-auto mb-8">Every wedding is unique, and I'm happy to create a custom package tailored to your specific needs and preferences. Contact me to discuss your vision, and we can craft the perfect coverage for your special day.</p>
      <div class="text-center">
        <%= link_to "Request Custom Package", pages_contact_path, class: "btn-gold" %>
      </div>
    </div>
  </div>
</section >
