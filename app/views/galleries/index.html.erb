<div class="container mx-auto">
  <ul>
    <% @galleries.each_with_index do |gallery, index| %>
      <li class="text-4xl font-bold"><%= "#{index + 1} - #{gallery.title}"  %></li>
    <% end %>
  </ul>
</div>
